# Requirements Ledger

One checkbox per requirement: explicit, implicit, and edge case. Written before
delegation. Checked off only after verification, never on a worker's assertion.
Archive a closed topic by renaming this file to `LEDGER-<topic>-archive.md`.

Legend: `[ ]` open, `[x]` verified, `[~]` deferred with user approval, plus the
date the user approved it.

## Explicit — the user said it

## Implicit — the code demands it

## Edge cases

## Deferred
