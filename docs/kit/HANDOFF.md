- 010-chunked-execution — ACTIVE, wave 1 of 2 landed 2026-09-20. Goal: the chair names a plan's real wave boundaries at confirmation time and the sponsor chooses to break there or run end-to-end; a closing retro leaves the next session a budgeted handoff brief. SHIPPED: T1 `1864e63` (break-point rule + offer in `plan/SKILL.md` step 4, `break point` -> `natural pause point` glossary row, `Natural pause points:` line in the sponsor preview template, both `.claude/templates/` seeds mirrored); T2 `bbb35b4` (`retro` writes `docs/kit/HANDOFF.md` via `.claude/hooks/lib/handoff.sh`'s guarded `handoff_write`, 27-case suite, `install-check.sh` now globs its test list). AC-1, AC-2, AC-4 demonstrated. Ledger E1-E3, I1-I4, I6, X1-X3 verified. OPEN: T3 (`intake` reads this file; builder, reviewer lane) and T4 (two-chunk fixture, AC-5; builder, critic lane) — I5 and X4 still open. NEXT: dispatch T3 first, and take AC-3's with/without timing from THIS session's own first turn — this file now exists, so a baseline measured from a session already carrying wave 1's context would be meaningless. Briefs in `docs/plans/010-chunked-execution.md` need no revision.
- Carried, not blocking — LEDGER-010's deferred item (an automated token/turn budget per plan) has no approval date; `trace.sh` reports it every run. Ask the sponsor to date it, or drop the deferral.
- Standing gotcha, hit twice on 2026-09-20 — `30-commit-gate.sh` resets its dispatch window on ANY commit, so the second and later commits of a session need `.claude/scripts/direct-lane.sh "<task>" "<reason>"` with an honest reason first, even when the work genuinely was dispatched and reviewed.
