- 010-chunked-execution — DONE 2026-09-20, closed and archived-ready. Goal: the chair names a plan's real wave boundaries at confirmation time and the sponsor chooses to break there or run end-to-end; a closing retro leaves a budgeted handoff brief. All 5 ACs demonstrated, all 4 success criteria met. Wave 1 `1864e63`/`bbb35b4`, wave 2 `2fabd17`, plus `406dd3a` (retro step3→step4 tests), `07a825d` (break-point disqualifier), `cc93eb0` (chair-model label). THE RESULT TO KNOW: chunking silently corrupts the acceptance record when one `AC-n` or ledger item is claimed by tasks on BOTH sides of the break — the closing chunk ticks it on half the evidence, the resuming chunk appends instead of correcting. Fixed by disqualifying such boundaries; run `.claude/scripts/break-points.sh <plan.md>` rather than deriving waves by hand. Under the fixed rule `plans/007-chair-overhead.md` has NO safe break point, and 001 drops 5→1 — do not assume a plan is splittable.
- Open, needing their own plan (LEDGER-010 F1, F2) — F1: `intake` step 1's `docs/kit/LEDGER*.md` glob also matches the six `-archive.md` files; pure first-turn cost, no correctness risk, dropped from T3's commit as scope creep. F2 (the bigger, and the natural successor): `10-session-start.sh` does not inject `docs/kit/HANDOFF.md`, so intake's read is the only path to it; injecting it would make that read redundant and move the saving into the session's most expensive turn.
- Carried, unresolved — LEDGER-010's deferred item (automated token/turn budget per plan) is dated 2026-09-24 as a REVISIT MARKER, not a reasoned deferral; the sponsor said "will review later". `trace.sh` reads it as approved. Re-ask rather than treating it as settled.
- Standing gotchas — `30-commit-gate.sh` resets its dispatch window on ANY commit, so the 2nd+ commits of a session need `.claude/scripts/direct-lane.sh "<task>" "<reason>"` first, with an honest reason. And `trace.sh`'s "every criterion has a block" counts a `[FAIL]` block as proven, so read verdicts, not the clean bill.
- Repo note, unfiled — `docs/kit/HANDOFF.md` is a regular file in `framework` while every other `docs/kit/*` is a symlink into `framework-dev`, so `framework-dev` holds no copy of its own handoff brief. Deliberate or an oversight from the session that created it; decide before it drifts further.
