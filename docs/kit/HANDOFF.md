- 013-phase-accounting — DONE 2026-09-22. Goal: `session-tokens.sh` shows what share of the chair's turns were fixed ritual (setup, close) versus handling dispatches (one orchestration row each) or direct-lane work, without a hand-read transcript. Shipped framework `14810f1` (block + self-test fixtures + METRICS.md row); records `cfed013` in framework-dev. All 4 ACs PASS on a closed session (23b81720) and a live one. KNOW THIS: to match the windows block exactly (AC-2), a dispatch's orchestration row absorbs chair turns back to the previous commit, and setup reads 0 when the first landmark is a commit. Decision 2 (sponsor): direct-lane = committed windows only; the trailing ceiling row is reconciled by a note line. Open: LEDGER-013 F1 (dedupe four copies of range-pricing awk), F2 (cross-session rollup stays manual).
- Open, needing their own plan (LEDGER-010 F1, F2) — unchanged this session. F1: `intake`'s `docs/kit/LEDGER*.md` glob also matches `-archive.md` files (first-turn cost only). F2: `10-session-start.sh` does not inject `docs/kit/HANDOFF.md`; injecting it would save intake a read in the most expensive turn.
- Carried, unresolved — LEDGER-010's deferred item (automated token/turn budget per plan) is dated 2026-09-24 as a REVISIT MARKER, not a reasoned deferral. Re-ask the sponsor rather than treating it as settled.
- Standing gotchas — `30-commit-gate.sh` resets its dispatch window on ANY commit (hit again this session; logged an honest `direct-lane.sh` reason). `trace.sh`'s clean bill counts a `[FAIL]` block as proven — read verdicts. `test-metrics.sh` greps `session-tokens.sh`'s whole report, so new report wording can break it.
- Repo note, unfiled — `docs/kit/HANDOFF.md` is a regular file in `framework` while other `docs/kit/*` are symlinks into `framework-dev`; decide whether that is deliberate. Also: this session ran the chair on opus while settings pin fable, so its 62% opus share is not a baseline.
