# Lessons

Newest first. One lesson is one bullet starting at column 0; it may wrap, as
long as the continuation lines are indented. Maximum 5 per session. Prune stale
lessons — one that no longer applies costs tokens in every future session. The
SessionStart hook carries the newest 12 and reports what it left behind, so keep
this file near that.
